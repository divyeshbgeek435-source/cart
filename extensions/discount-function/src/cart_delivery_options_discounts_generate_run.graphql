query DeliveryInput {
  cart {
    deliveryGroups {
      id
    }
  }
  discount {
    discountClasses
    metafield(namespace: "default", key: "function-configuration") {
      jsonValue
    }
  }
}
